[project]
name = "timebasedris"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "gmarthe", email = "guillaume.marthe@wanadoo.fr" }
]
requires-python = ">=3.13"
dependencies = []

[project.scripts]
timebasedris = "timebasedris:main"

[build-system]
requires = ["uv_build>=0.12.7,<0.13.0"]
build-backend = "uv_build"
